{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://aidb.local/skins/_schema.json",
  "title": "Caption Skin",
  "description": "A complete captioning recipe. Source fields are authored by humans; the `_built` block is written by `python -m ait.caption.skin_build <name>` and read by use-time consumers (Skin.load, JoyNG, JoySceneDBNG, /v1-status, /todo-ai). Never hand-edit `_built`.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "name",
    "version",
    "entities",
    "default_prompt",
    "user_hint_preamble",
    "post_prompt",
    "concepts",
    "model"
  ],
  "properties": {
    "name": {
      "description": "Skin identifier; must equal the JSON filename stem.",
      "type": "string",
      "pattern": "^[A-Za-z0-9_-]+$"
    },
    "version": {
      "description": "Schema version this skin was authored against.",
      "type": "integer",
      "minimum": 1
    },
    "description": {
      "type": "string"
    },
    "entities": {
      "type": "object",
      "additionalProperties": false,
      "required": ["primary"],
      "properties": {
        "primary":   { "$ref": "#/$defs/entity" },
        "secondary": { "oneOf": [{ "$ref": "#/$defs/entity" }, { "type": "null" }] }
      }
    },
    "interaction": {
      "oneOf": [{ "$ref": "#/$defs/interaction" }, { "type": "null" }]
    },
    "default_prompt": {
      "type": "string"
    },
    "user_hint_preamble": {
      "description": "Wrapper string for per-image user hints; must contain {hint}.",
      "type": "string"
    },
    "post_prompt": {
      "type": "string"
    },
    "body_type_words": {
      "description": "Map of authorizing-label -> list of words that may only appear in captions when the label is set. Keys starting with `_` are reserved for human-readable annotations and are ignored by the loader.",
      "type": "object",
      "patternProperties": {
        "^_[A-Za-z0-9_]*$": { "type": "string" },
        "^(?!_)[A-Za-z0-9_]+$": {
          "type": "array",
          "items": { "type": "string", "minLength": 1 }
        }
      },
      "additionalProperties": false
    },
    "require_trigger_presence": {
      "description": "When true, validators flag captions missing both entity phrases.",
      "type": "boolean",
      "default": false
    },
    "concepts": {
      "description": "Composite categories used by /v1-status and /todo-ai. Keys are concept names; values are match rules.",
      "type": "object",
      "additionalProperties": { "$ref": "#/$defs/concept" }
    },
    "model": {
      "$ref": "#/$defs/install_ref",
      "description": "AInstallerDB tuple for the base captioning model."
    },
    "lora": {
      "oneOf": [{ "$ref": "#/$defs/install_ref" }, { "type": "null" }],
      "description": "Optional AInstallerDB tuple for a fine-tuned LoRA adapter; null = no LoRA."
    },
    "default_set": {
      "oneOf": [{ "type": "string" }, { "type": "null" }],
      "description": "SceneSet most associated with this skin; default for /v1-status and /todo-ai."
    },
    "_built": {
      "oneOf": [{ "$ref": "#/$defs/built" }, { "type": "null" }],
      "description": "Derived state written by the build phase. Optional; loader warns if missing or stale."
    }
  },

  "$defs": {
    "entity": {
      "type": "object",
      "additionalProperties": false,
      "required": ["token", "phrase"],
      "properties": {
        "description": { "type": "string" },
        "token": {
          "description": "Nonsense-word handle anchored by the LoRA, e.g. 'xlgts'.",
          "type": "string",
          "minLength": 1
        },
        "phrase": {
          "description": "Token + descriptor noun, e.g. 'xlgts woman'. Used in label-prompt interpolation.",
          "type": "string",
          "minLength": 1
        },
        "rules": {
          "type": "array",
          "items": { "type": "string", "minLength": 1 }
        },
        "forbidden": {
          "description": "Phrases the captioner must not produce when this entity is in scope.",
          "type": "array",
          "items": { "type": "string", "minLength": 1 }
        },
        "label_groups": {
          "type": "object",
          "additionalProperties": { "$ref": "#/$defs/label_group" }
        }
      }
    },

    "interaction": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "description": { "type": "string" },
        "rules": {
          "type": "array",
          "items": { "type": "string", "minLength": 1 }
        },
        "forbidden": {
          "type": "array",
          "items": { "type": "string", "minLength": 1 }
        },
        "label_groups": {
          "type": "object",
          "additionalProperties": { "$ref": "#/$defs/label_group" }
        }
      }
    },

    "label_group": {
      "type": "object",
      "additionalProperties": false,
      "required": ["labels"],
      "properties": {
        "description": { "type": "string" },
        "labels": {
          "description": "Array of label objects in declaration order. Each label has a unique `name`, an optional human-readable `description`, and an `expansion` template (placeholders use {entities.primary.phrase} etc.).",
          "type": "array",
          "items": { "$ref": "#/$defs/label" }
        }
      }
    },

    "label": {
      "type": "object",
      "additionalProperties": false,
      "required": ["name", "expansion"],
      "properties": {
        "name": {
          "description": "Unique label identifier (used as the SceneImage label key).",
          "type": "string",
          "pattern": "^[A-Za-z0-9_]+$"
        },
        "description": {
          "description": "Human-readable note: what this label means and when to apply it.",
          "type": "string"
        },
        "expansion": {
          "description": "Label-prompt template — the sentence inserted into the model prompt when this label is on an image. Placeholders are interpolated at build time.",
          "type": "string"
        },
        "migration": {
          "description": "Accumulating log of migration notes. Each entry is a free-text string prefixed with an ISO-8601 CET/CEST timestamp with seconds, e.g. `'2026-05-10T20:30:42+02:00: renamed from b_muscular; SceneImages tagged with the old name should be relabeled before training'`. New notes are appended; the list is cleared only by a manual flush (TBD).",
          "type": "array",
          "items": { "type": "string", "minLength": 1 }
        }
      }
    },

    "concept": {
      "type": "object",
      "additionalProperties": false,
      "anyOf": [
        { "required": ["labels"] },
        { "required": ["label_prefix"] },
        { "required": ["residual"] }
      ],
      "properties": {
        "description":         { "type": "string" },
        "labels":              { "type": "array", "items": { "type": "string", "minLength": 1 } },
        "label_prefix":        { "type": "array", "items": { "type": "string", "minLength": 1 } },
        "residual":            { "type": "boolean" },
        "ignore_labels":       {
          "description": "For residual concepts: labels in this list don't qualify an image for the residual match (e.g. attribute-only images shouldn't trigger `general`).",
          "type": "array",
          "items": { "type": "string", "minLength": 1 }
        },
        "ignore_label_prefix": {
          "description": "For residual concepts: labels with these prefixes don't qualify an image for the residual match.",
          "type": "array",
          "items": { "type": "string", "minLength": 1 }
        },
        "target":              { "type": "integer", "minimum": 0 },
        "sub_concepts": {
          "type": "object",
          "additionalProperties": { "$ref": "#/$defs/concept" }
        }
      }
    },

    "install_ref": {
      "type": "object",
      "additionalProperties": false,
      "required": ["group", "variant", "target"],
      "properties": {
        "group":   { "type": "string", "minLength": 1 },
        "variant": { "type": "string", "minLength": 1 },
        "target":  { "type": "string", "minLength": 1 }
      }
    },

    "built": {
      "type": "object",
      "additionalProperties": false,
      "required": ["version", "built_at", "source_hash", "directive", "labels", "label_to_group", "label_to_entity", "forbidden"],
      "properties": {
        "version":     { "type": "integer", "minimum": 1 },
        "built_at":    { "type": "string", "description": "ISO-8601 UTC timestamp." },
        "source_hash": { "type": "string", "description": "SHA-256 of the source fields used to compose this build." },
        "directive": {
          "description": "Composed canonical recipe text sent to both system and user roles.",
          "type": "string"
        },
        "labels": {
          "description": "Flat lookup of label PATH -> rendered (placeholder-interpolated) sentence. Paths are `<entity>.<group>.<leaf>`.",
          "type": "object",
          "patternProperties": {
            "^[A-Za-z0-9_]+\\.[A-Za-z0-9_]+\\.[A-Za-z0-9_]+$": { "type": "string" }
          },
          "additionalProperties": false
        },
        "label_to_group":  {
          "description": "Reverse index: label path -> 'primary.<group>' / 'secondary.<group>' / 'interaction.<group>'.",
          "type": "object",
          "patternProperties": {
            "^[A-Za-z0-9_]+\\.[A-Za-z0-9_]+\\.[A-Za-z0-9_]+$": { "type": "string" }
          },
          "additionalProperties": false
        },
        "label_to_entity": {
          "description": "Reverse index: label path -> 'primary' | 'secondary' | 'interaction'.",
          "type": "object",
          "patternProperties": {
            "^[A-Za-z0-9_]+\\.[A-Za-z0-9_]+\\.[A-Za-z0-9_]+$": { "type": "string", "enum": ["primary", "secondary", "interaction"] }
          },
          "additionalProperties": false
        },
        "forbidden": {
          "description": "Union of primary.forbidden + secondary.forbidden + interaction.forbidden.",
          "type": "array",
          "items": { "type": "string", "minLength": 1 }
        }
      }
    }
  }
}
