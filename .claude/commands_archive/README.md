# Archived slash commands (deactivated 2026-08-21)

Captioning/curation commands parked during the aitools agent refocus
(library + app maintenance). They are NOT loaded by Claude Code from this
directory — zero context cost — but kept intact because captioning will be
done differently in the future and these document the current recipes.

Reactivate a command by moving its `.md` back to `.claude/commands/`.

Companion knowledge lives in the memory store's `archive_captioning/`
subfolder (same deal: kept, not auto-loaded).

`img_prompt_enh` was retired outright (not archived) the same day.
